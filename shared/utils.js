/**
 * Utility helpers for Secret Hitler game logic.
 */

/**
 * Shuffles an array in place and returns it.
 * @param {Array} deck
 */
function shuffleDeck(deck) {
  // TODO: implement Fisher-Yates shuffle
  return deck;
}

/**
 * Assigns roles to players based on player count.
 * @param {Array} playerList
 */
function assignRoles(playerList) {
  // TODO: use ROLE_DISTRIBUTION to assign roles
  return playerList;
}

/**
 * Generates a unique room code.
 */
function generateRoomCode() {
  // TODO: create random room code
  return 'ABCD';
}

module.exports = {
  shuffleDeck,
  assignRoles,
  generateRoomCode,
};
